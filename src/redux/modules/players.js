
const initState = {};

export const types = {
    dataList:[],
    total:0,
    other:{

    }
}

export const actions = {

}



const dataList = (state=initState.dataList,action) => {
    return state;
}

export default reducer 